package com.ginkgocap.ywxt.metadata.dao;

import java.util.List;

import com.ginkgocap.ywxt.metadata.model.EmailSuffix;

public interface EmailSuffixDao {
	/**
     * 通过主键获得邮箱后缀
     * @param id
     * @return
     */
	EmailSuffix findOne(long id);
    /**
     * 查询所有的邮箱后缀
     * @param group
     * @param level
     * @return
     */
    List<String> findAll();
    /**
     * 插入或者更新邮箱后缀
     * @param code
     * @return
     */
    EmailSuffix saveOrUpdate(EmailSuffix emailSuffix);
    /**
     * 删除邮箱后缀
     * @param id
     */
    void delete(long id);
}
