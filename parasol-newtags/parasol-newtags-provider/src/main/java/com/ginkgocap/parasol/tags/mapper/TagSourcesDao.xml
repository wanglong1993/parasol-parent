<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ginkgocap.parasol.tags.mapper.TagSourcesDao">

    <delete id="deleteSourceByType" >
        DELETE FROM tb_tag_source where userId=#{userId} AND tagId = #{tagId}
        <if test=" sourceType!=0 ">
            and sourceType = #{sourceType}
        </if>
    </delete>

    <select id="selectSourceByTagId"  resultType="tagSource">
        select * from tb_tag_source where 1=1
        <if test=" userId!=0 ">
            and userId=#{userId}
        </if>
        <if test=" sourceType!=0 ">
            and sourceType = #{sourceType}
        </if>
        <if test=" tagId!=0">
            and tagId = #{tagId}
        </if>
        <if test=" keyword!=null and keyword!='' ">
            and sourceTitle LIKE CONCAT(CONCAT('%', #{keyword}), '%')
        </if>
        GROUP BY sourceId,sourceType
        order by createAt DESC
        limit #{index},#{size}
    </select>

    <select id="countSourceByTagId"  resultType="java.lang.Long">
        SELECT COUNT(*) from tb_tag_source where 1=1
        <if test=" userId!=0 ">
            and userId=#{userId}
        </if>
        <if test=" sourceType!=0 ">
            and sourceType = #{sourceType}
        </if>
        <if test=" tagId!=0">
            and tagId = #{tagId}
        </if>
        <if test=" keyword!=null and keyword!='' ">
            and sourceTitle LIKE CONCAT(CONCAT('%', #{keyword}), '%')
        </if>
--         GROUP BY sourceId,sourceType
    </select>

    <select id="searchSourcesExctTag"  resultType="tagSource">
        select * from tb_tag_source where 1=1
        <if test=" userId!=0 ">
            and userId=#{userId}
        </if>
        <if test=" sourceType!=0 ">
            and sourceType = #{sourceType}
        </if>
        <if test=" keyword!=null and keyword!='' ">
            and sourceTitle LIKE CONCAT(CONCAT('%', #{keyword}), '%')
        </if>
        and tagId not IN (
             SELECT id FROM tb_tag where userId=#{userId} and tagName LIKE CONCAT(CONCAT('%', #{keyword}), '%')
        )
        GROUP BY sourceId,sourceType
        order by createAt DESC
    </select>

    <insert id="insertSource" parameterType="tagSource">
        INSERT INTO tb_tag_source (id,tagId,appId,userId,sourceId,sourceType,createAt,sourceTitle,sourceExtra)
        VALUES (#{id},#{tagId},#{appId},#{userId},#{sourceId},#{sourceType},#{createAt},#{sourceTitle},#{sourceExtra})
    </insert>

</mapper>