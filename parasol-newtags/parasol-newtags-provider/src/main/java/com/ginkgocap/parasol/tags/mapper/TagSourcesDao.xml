<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ginkgocap.parasol.tags.mapper.TagSourcesDao">

    <select id="searchTagSources"  resultType="tagSource">
        select a.* from tb_tag_source a left join tb_tag b on a.userId = b.userId where 1=1
        <if test=" userId!=0 ">
            and a.userId=#{userId}
        </if>
        <if test=" sourceType!=0 ">
            and a.sourceType = #{sourceType}
        </if>
        <if test=" keyWord!='' and keyWord!=null">
            and a.sourceTitle LIKE concat(concat('%',#{keyWord}),'%')
        </if>
        order by a.createtime desc
        limit #{index},#{size}
    </select>
</mapper>